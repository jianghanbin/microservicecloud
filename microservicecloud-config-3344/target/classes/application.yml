server:
   port: 3344

spring:
   application:
      name: microservicecloud-config
   cloud:
      config:
         server:
            git:
               uri: git@github.com:jianghanbin/microservicecloud-config.git   #github上面的项目地址